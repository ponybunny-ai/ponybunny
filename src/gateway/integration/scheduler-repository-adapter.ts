/**
 * Compatibility shim for callers that still import the adapter from the
 * historical gateway integration path.
 */

export { SchedulerRepositoryAdapter } from '../../scheduler/composition/scheduler-repository-adapter.js';
