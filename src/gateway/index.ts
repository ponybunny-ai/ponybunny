/**
 * Historical mixed gateway barrel.
 *
 * Prefer `./public.js` for the intended live gateway surface and
 * `./compatibility.js` for import-preserving compatibility exports.
 */

export * from './public.js';
export * from './compatibility.js';
