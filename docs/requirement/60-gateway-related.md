1. Gateway 服务需要向外暴露一个系统状态的接口，web目录下的前端页面可以读取并显示当前系统的各项运行参数。
    1.1 系统状态需要包括操作系统和硬件的各项参数（CPU，内存，网络接口，操作系统名称，版本等），当前gateway的进程信息，scheduler的进程信息等。
    1.2 gateway的连接情况：client的总数，每个client的基本信息等。
    1.3 通过gateway暴露后面其他进程的状态。
    1.4 gateway要有监控配置文件的变化并且自动重启的能力。