# 当前bug清单

1. TUI 界面显示bug。 dashboard 顶部的内容缺少。 对比 Sesseion 和 Workstream 等view，缺内容（最左上角的Ponybunny不见了，  | S hello (eedf488c) | M CPA.gpt-5.4 | I session-first 这些信息不见了）
2. TUI 左下角显示当前 Agent 的地方显示错误。 应该默认显示用户在配置文件里配置的默认agnet（系统初始化时是lead），但是现在显示的是forge。
3. 对话行为出错。 本来应该用户输入内容后，在对话显示区域立刻显示出来。 但是现在直接界面卡死什么都不显示。 要等很久之后才会显示出来。
4. 依然没有正确识别用户对话的意图并分解任务等动作。 也就是说，现在依然是断层的。