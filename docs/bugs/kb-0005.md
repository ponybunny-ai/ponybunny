# TUI 对话输入框布局、配色和功能的bug

## bug
1. ctrl-t的行为是切换模型。 期望是根据模型是否提供不同的variant来做切换，例如thinking的强度。
2. ctrl-p弹出的命令选择框里的命令列表不能自动滚动，导致后面的命令看不到也选不到。
3. 布局和配色问题。 输入框的背景为灰色，导致文字看不清。 输入框的布局也错误，应该将输入框和 `forge │ openai.gpt-5 <8/15> │ ctrl-t variants tab agents ctrl-v views ctrl-p commands` 放在同一个box里，并且使用相同的背景色。 将 `Dashboard  Tasks  Goals  Events` 放在下面一行里，右对齐，不加背景色。

## 当前的样子
```
┌────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐│ ➤ Describe your goal or type /help for commands                                                                                                                    │
└────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘  forge │ openai.gpt-5 <8/15> │ ctrl-t variants tab agents ctrl-v views ctrl-p commands                                              Dashboard  Tasks  Goals  Events

```