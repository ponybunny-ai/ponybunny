# PonyBunny Refactor Master Task List

Status values:
- `done`
- `in_progress`
- `planned`
- `blocked`
- `deferred`

Update rule from Session 22 onward:
- Every Codex implementation session must update this file.
- It must update the relevant task row(s): `status`, `last_session`, `notes`.
- It must not rewrite unrelated rows.
- It must keep task IDs stable.
- If a session creates a new follow-up task, it may append a new row in the appropriate section using the next available task ID.

## 1. Project Summary

| Area | Current State | Notes |
|---|---|---|
| Runtime event spine | done | Event bus, event store, tail/replay, terminology normalization completed. |
| Scheduler ↔ Execution decoupling | done | ExecutionPort, LocalExecutionWorker, mode switch, result handoff, continuation convergence completed. |
| Evented execution hardening baseline | done | Reconciliation, durable dispatch checkpoint, durable idempotency, orphan policy, operator inspection, recovery-candidate skeleton completed. |
| Manual recovery action model | done | Session 21 defined the model and Session 22 implemented the first metadata-only clear action. |
| ToolWorker extraction | done | Sessions 33-45 completed the intended local-authoritative ToolWorker line; broader non-local hardening and mode work remain deferred/planned. |
| Conversation/session decoupling | done | Session 53 closed the current local ConversationWorker line as stable enough to pause as the primary focus; broader non-local hardening and ownership migration remain deferred. |
| Deeper code-boundary cleanup | re-ranked | Import-cycle, infra-hub, compatibility/public-surface, startup/bootstrap, and GatewayServer wiring lines are closed; `RF-030`, `RF-036`, and `RF-071` remain paused after their late-stage reviews, and Sessions 122-124 have now started the next active block with an explicit read-only agent-definition/model-hint boundary for `SessionManager` and `WorkloadModelResolver`, while gateway materialization, daemon startup ownership, scheduler composition fallback, and runner registration remain intentionally out of scope pending review. Broader source-of-truth, singleton, detach, and topology-dependent tool follow-ups remain intentionally paused unless that narrower boundary work proves out. |

## 2. Task Table

| Task ID | Area | Task | Status | Priority | Depends On | Last Session | Notes |
|---|---|---|---|---|---|---|---|
| RF-001 | Runtime spine | Create RuntimeEvent abstraction and memory event bus | done | high | — | 2 | Completed in earlier playbook sessions. |
| RF-002 | Runtime spine | Bridge gateway/debug/scheduler events into runtime event bus | done | high | RF-001 | 4 | Adapters added and wired. |
| RF-003 | Runtime spine | Add runtime event persistence and CLI tail/replay | done | high | RF-001 | 7 | Event store and CLI inspection completed. |
| RF-004 | Terminology | Normalize new runtime spine terminology to `workItemId` / `work_item_id` | done | high | RF-003 | 10A | Narrow terminology unification completed. |
| RF-005 | Execution boundary | Introduce ExecutionPort / ExecutionRequest / ExecutionResult boundary | done | high | RF-004 | 10 | Scheduler now depends on ExecutionPort. |
| RF-006 | Execution worker | Add LocalExecutionWorker skeleton subscribed to `task.ready` | done | high | RF-005 | 11 | Worker exists in parallel, not initially authoritative. |
| RF-007 | Execution modes | Add `direct` vs `evented` execution mode switch | done | high | RF-006 | 12 | Default remains `direct`. |
| RF-008 | Evented execution | Make `execution.completed` / `execution.failed` authoritative in evented mode | done | high | RF-007 | 13 | Scheduler consumes worker result events. |
| RF-009 | Post-result continuation | Converge direct/evented post-execution continuation path | done | high | RF-008 | 14 | Shared continuation entry point added. |
| RF-010 | Failure semantics | Enrich `execution.failed` payload with richer failed result structure | done | medium | RF-009 | 15 | Better accounting and diagnostics in evented mode. |
| RF-011 | Hardening design | Produce evented hardening design doc | done | medium | RF-010 | 16 | Reliability gaps and roadmap documented. |
| RF-012 | Reconciliation | Add durable evented dispatch checkpoint and startup reconciliation skeleton | done | high | RF-011 | 17 | Conservative detection/classification only. |
| RF-013 | Idempotency | Add durable scheduler-side idempotency for evented result application | done | high | RF-012 | 18 | Duplicate result continuation suppressed durably. |
| RF-014 | Orphan policy | Add conservative timeout/orphan policy for stale evented runs | done | medium | RF-012 | 19 | Durable stale/orphan marking completed. |
| RF-015 | Operator inspection | Add read-only reconciliation/orphan inspection CLI surface | done | medium | RF-014 | 20A | In-flight/orphaned/reconciliation-summary queries added. |
| RF-016 | Recovery candidate skeleton | Add inspect-one-run and mark-recovery-candidate CLI workflow | done | medium | RF-015 | 20B | Conservative manual recovery preparation added. |
| RF-017 | Manual recovery design | Produce manual recovery action design doc | done | high | RF-016 | 21 | Session 21 documented clear/unmark as the first safe metadata-only action. |
| RF-018 | Manual recovery action 1 | Implement safest first manual recovery action chosen by RF-017 | done | high | RF-017 | 22 | `clear-recovery-candidate` clears only `evented_dispatch.recovery_candidate` and preserves direct mode semantics. |
| RF-019 | Manual recovery action 2 | Add second conservative manual recovery operator action if justified | done | medium | RF-018 | 23 | Session 23 added metadata-only `mark-replay-candidate` with `recovery_candidate` precondition and inspect support; no replay semantics were introduced. |
| RF-020 | Recovery readiness review | Reassess evented mode default-readiness after manual recovery baseline | done | medium | RF-018 | 30 | Session 30 completed the stage review: direct remains the safe default, evented is controlled-rollout ready but not default-ready, manual replay is guarded but not yet routine-use ready, and the next hardening set is daemon-safe replay plus replacement-run follow-up policy and tighter replay auditability. |
| RF-043 | Execution/recovery hardening | Close the minimum hardening set identified by RF-020 before broader evented adoption | done | high | RF-020 | 31 | Session 31 added a daemon-safe manual replay control path through the active scheduler daemon, preserved strict replay gating and duplicate suppression, and surfaced stable operator-facing policy state/reason when a replacement replay attempt later becomes stale/orphaned; direct mode remains unaffected. |
| RF-044 | Execution/recovery closure review | Produce a closure review and handoff recommendation for the execution/recovery line after Sessions 10-31 | done | medium | RF-043 | 32 | Session 32 concluded the execution/recovery line is stable enough to pause as the primary refactor focus: direct remains the default, evented/manual replay/daemon-safe replay are guarded operator tools, broader/default adoption still needs short-tail audit and workflow validation, and Session 33 should start ToolWorker extraction design. |
| RF-021 | Tool boundary design | Produce ToolWorker extraction design and dependency map | done | medium | RF-009 | 33 | Session 33 documented the current tool-execution path, dependency hotspots, concern split, narrow first ToolPort/ToolRequest/ToolResult boundary, preserved execution/recovery invariants, and recommended Session 34 as direct in-process tool boundary extraction without changing scheduler, IPC, or tool semantics. |
| RF-022 | Tool boundary extraction | Introduce ToolPort / tool execution boundary | done | medium | RF-021 | 34 | Session 34 added a narrow in-process `ToolPort` / `ToolRequest` / `ToolResult` boundary, a `LocalToolAdapter` over the existing `ToolEnforcer` + `ToolRegistry` path, and routed `ReActIntegration` tool calls through it without changing gateway, IPC, policy ownership, prompt/schema generation, or direct/evented semantics. |
| RF-023 | Tool worker skeleton | Add ToolWorker skeleton in parallel path | done | medium | RF-022 | 35 | Session 35 added a local in-process `LocalToolWorker` that reuses `ToolPort` / `ToolRequest` / `ToolResult`, emits `tool.requested` / `tool.started` / `tool.completed` / `tool.failed`, suppresses duplicate `toolRequestId`s in memory, and is composed in parallel without changing gateway, IPC, prompt/schema generation, policy ownership, or the default direct tool path. |
| RF-045 | Tool handoff design | Produce the first tool result handoff design for future authoritative ToolWorker execution | done | medium | RF-023 | 36 | Session 36 documented the current direct tool-result path, defined continuation ownership and correlation requirements for a future worker-driven path, preserved execution/recovery invariants, and recommended Session 37 as the smallest safe coding step: switch `ReActIntegration` to await local in-process `LocalToolWorker.dispatch(...)` without adding a tool mode switch. |
| RF-024 | Tool mode switch | Add direct/evented tool dispatch mode if needed | planned | low | RF-023 | 39 | Session 39 documented the first safe tool mode switch design and concluded a formal mode setting should not exist yet; any future implementation must preserve one awaited `ToolResult` and keep continuation ownership inside `ReActIntegration`. |
| RF-025 | Tool result handoff | Make tool result path scheduler/execution-safe and idempotent | done | medium | RF-045 | 45 | Session 38 hardened the authoritative local seam with request-identity validation, broader result-correlation checks, invalid-result normalization, and narrow in-process inspection visibility; Session 40 then documented the promise-bridged authoritative handoff model; Session 41 added the first in-process request registry so the authoritative local path now registers by `toolRequestId`, returns one registry-owned promise, and suppresses duplicate terminal completion without changing continuation ownership or scheduler-facing semantics; Session 42 then documented the first narrow timeout / missing-result design; Session 43 then implemented that local timeout normalization in `LocalToolWorker`, resolving the registry-owned promise with one normalized failed `ToolResult` using `TOOL_EXECUTION_TIMEOUT` and ignoring late completions for continuation purposes; Session 44 then added explicit terminal-path and late-result diagnostics on the same local path; Session 45 closes the local handoff line as stable enough to pause as the primary focus. |
| RF-026 | Tool hardening | Add recovery/idempotency/inspection for tool worker path | deferred | low | RF-025 | 45 | Session 38 started the local inspection/hardening slice by adding read-only `LocalToolWorker.inspect()` diagnostics plus inspection summaries on `tool.*` runtime events; Session 44 extended that local inspection surface with explicit timeout/late-completion/invalid-completion metadata, terminal-path visibility, and narrow summary counters while keeping everything local and in-process; Session 45 concluded that broader durable recovery/idempotency, operator-facing inspection, and cross-process hardening are not immediate blockers and can be deferred until a non-local tool topology is justified. |
| RF-046 | Tool mode switch design | Produce the first safe tool mode switch design for future worker-driven tool execution | done | medium | RF-025 | 39 | Session 39 documented the current authoritative local tool path, defined the minimum invariant set for any future switch, rejected a formal `toolExecutionMode` setting for now, and recommended Session 40 as a promise-bridged handoff design before any implementation work. |
| RF-047 | Tool promise bridge prototype | Prototype a narrow local internal request registry for promise-bridged authoritative tool handoff | done | medium | RF-025 | 41 | Session 41 implemented the narrow local in-process `ToolRequestRegistry`, wired `LocalToolWorker` through registry-owned promise registration before execution, preserved the await-based `ReActIntegration` contract, recorded terminal metadata by `toolRequestId`, and kept gateway behavior, IPC, MCP/local execution compatibility, and scheduler-facing semantics unchanged. |
| RF-048 | Local tool timeout normalization | Add the first narrow timeout / missing-result resolution path for registered local tool requests | done | medium | RF-025 | 43 | Session 42 defined the smallest safe implementation; Session 43 completed it by having `LocalToolWorker` own one local timeout per authoritative registration, resolve the registry-owned promise with a normalized failed `ToolResult` using `TOOL_EXECUTION_TIMEOUT`, preserve request identity fields, and treat late results as diagnostic-only without changing gateway, IPC, evented semantics, or scheduler ownership. |
| RF-049 | ToolWorker closure review | Produce a closure review and handoff recommendation for the ToolWorker line after Sessions 33-44 | done | medium | RF-025 | 45 | Session 45 concluded the ToolWorker line is stable enough to pause as the primary refactor focus for its intended local-authoritative scope: `LocalToolWorker` is the authoritative local seam, request-registry handoff and timeout normalization now protect one awaited continuation per `toolRequestId`, broader durable/cross-process hardening remains intentionally deferred, and Session 46 should begin ConversationWorker extraction design. |
| RF-050 | Conversation request registry design | Produce the first local conversation request-registry design for the new ConversationWorker seam | done | medium | RF-031 | 49 | Session 49 documented the current local conversation handoff model, identified the lifecycle and ownership gaps around duplicate suppression, request identity, terminal resolution, invalid/late/missing-result handling, and concluded that a narrow local in-process `ConversationRequestRegistry` is now justified as the smallest safe lifecycle owner under `ConversationWorker`; Session 50 should implement that prototype without changing gateway behavior, IPC, continuation ownership, or scheduler-authoritative task materialization. |
| RF-051 | Conversation request registry prototype | Implement the first narrow local in-process `ConversationRequestRegistry` under the authoritative `ConversationWorker` seam | done | medium | RF-050 | 50 | Session 50 added a local in-process `ConversationRequestRegistry` keyed by `conversationRequestId`, moved duplicate in-flight suppression and registry-owned promise lifecycle ownership under that seam, kept `SchedulerSessionIntake` as the outer await/continuation owner, preserved gateway behavior and scheduler-authoritative task materialization, and left timeout/evented/durable conversation behavior intentionally out of scope. |
| RF-052 | Conversation timeout / missing-result design | Produce the first timeout / missing-result design for the local `ConversationWorker` request-registry handoff path | done | medium | RF-051 | 51 | Session 51 documented the current local request-registry handoff model, identified the remaining indefinite-wait and late-completion risk after registration, recommended `ConversationWorker` as the first timeout-policy owner, recommended rejecting the registry-owned promise with a normalized `CONVERSATION_EXECUTION_TIMEOUT` failure rather than widening `ConversationResult`, defined late results after timeout as diagnostic-only and non-authoritative, and recommended Session 52 implement that narrow local timeout normalization without changing gateway behavior, IPC, continuation ownership, or scheduler-authoritative task materialization. |
| RF-053 | Conversation timeout normalization | Implement the first narrow local timeout / missing-result resolution path for registered local conversation requests | done | medium | RF-052 | 52 | Session 52 added one worker-owned local timer in `ConversationWorker` after authoritative registration, rejects the registry-owned promise through the existing failure path with `CONVERSATION_EXECUTION_TIMEOUT`, preserves request identity on the timeout error, keeps exactly one terminal outcome per `conversationRequestId`, and treats late completions as local inspection-only diagnostics without changing gateway behavior, IPC, continuation ownership, or scheduler-authoritative task materialization. |
| RF-054 | ConversationWorker closure review | Produce a closure review and handoff recommendation for the ConversationWorker line after Sessions 46-52 | done | medium | RF-053 | 53 | Session 53 concluded the ConversationWorker line is stable enough to pause as the primary refactor focus for its intended local-authoritative scope: `ConversationWorker` is the authoritative local message-execution seam, the request-registry handoff and timeout normalization now protect one awaited continuation per `conversationRequestId`, broader durable/cross-process hardening remains intentionally deferred, and Session 54 should begin runtime boundary cleanup work instead of broadening conversation topology. |
| RF-027 | Conversation design | Produce ConversationWorker / session decoupling design doc | done | medium | RF-016 | 46 | Session 46 documented the current gateway -> IPC -> scheduler intake -> SessionManager path, mapped dependency/coupling hotspots, defined the first safe local `ConversationPort` / `ConversationRequest` / `ConversationResult` boundary, kept scheduler-owned task materialization and gateway routing outside the worker, and recommended Session 47 as narrow local boundary extraction without changing IPC, gateway behavior, or execution/recovery semantics. |
| RF-028 | Conversation boundary extraction | Introduce conversation/session boundary port | done | medium | RF-027 | 47 | Session 47 added a narrow local `ConversationPort` / `ConversationRequest` / `ConversationResult` seam with worker-local `conversationRequestId`, and routed `SchedulerSessionIntake.processMessage(...)` through it without changing gateway behavior, IPC, scheduler task materialization authority, repository ownership, or gateway session routing ownership. |
| RF-029 | Conversation worker skeleton | Add ConversationWorker skeleton | done | medium | RF-028 | 47 | Session 47 added the first local in-process `ConversationWorker` wrapping the existing `SessionManager` orchestration path; it is authoritative only for the narrow local conversation seam and does not introduce multi-process dispatch, durable ledgers, or ownership changes across scheduler, gateway, or persistence boundaries. |
| RF-030 | Conversation materialization decoupling | Decouple session-to-goal materialization from direct scheduler bridge | deferred | medium | RF-029 | 114 | Session 112 started the line with the bounded review in `docs/refactoring/session112-rf030-materialization-review.md`; Session 113 then completed the first major coding cluster in `docs/refactoring/session113-rf030-materialization-owner-extraction.md` by introducing scheduler-daemon-owned `ConversationTaskMaterializer`, moving the concrete goal creation, first work-item creation, selected-model compatibility projection, and scheduler-submit sequence out of `SchedulerTaskBridge`; Session 114 then completed the bounded line review in `docs/refactoring/session114-rf030-line-review.md` and concluded the remaining candidates are lower-yield observation/naming/bootstrap follow-ups rather than another clearly high-value bounded cluster, so RF-030 should pause and be re-ranked. |
| RF-031 | Conversation hardening | Add inspection/reliability model for conversation worker path | done | low | RF-030 | 48 | Session 48 hardened the first local ConversationWorker seam with request validation, intake-side result identity checks, exact duplicate in-flight suppression by `conversationRequestId`, and a read-only local inspection snapshot exposed through the worker/intake without changing gateway behavior, IPC, scheduler task materialization authority, or session routing ownership. |
| RF-032 | Boundary cleanup | Reduce global singleton dependence in runtime core | done | medium | RF-009 | 58 | Session 54 finalized the design baseline in `docs/refactoring/session54-runtime-core-boundary-cleanup-design.md`, Session 55 introduced the first explicit `RuntimeToolingContext` boundary for execution/planning/conversation consumers, Session 56 threaded that context through the scheduler-daemon bootstrap/session-intake composition path, Session 57 made the migrated daemon/session-intake path require explicit `RuntimeToolingContext`, and Session 58 closed the current line as stable enough to pause as the primary focus: runtime-owned tooling shape is now explicit on migrated paths, compatibility globals remain deferred mirrors/fallback surfaces, and broader singleton removal is intentionally not yet in scope. |
| RF-033 | Import-cycle cleanup | Break major cross-layer import cycles in `src/` | done | medium | RF-032 | 68 | Session 60 introduced the first narrow runtime-owned `ExecutionRunner` boundary in `src/runtime/execution-boundary/execution-runner.ts`; Session 61 then added the contained runtime-facing `ExecutionCycleRunner` seam in `src/runtime/execution-boundary/execution-cycle-runner.ts`; Session 62 introduced `ExecutionCycleRuntimeFactory` in `src/runtime/execution-boundary/execution-cycle-runtime-factory.ts`, so `ExecutionService` no longer directly assembles the default `RuntimeToolingContext` plus `LocalExecutionCycleRunner` knot; Session 63 then introduced `ExecutionToolPolicyPreparer` in `src/runtime/execution-boundary/execution-tool-policy-preparer.ts`, so `ExecutionService` no longer directly performs the scoped per-work-item tool-policy preparation; Session 64 continued that cleanup with `ExecutionResourcePreparer` in `src/runtime/execution-boundary/execution-resource-preparer.ts`; Session 65 then introduced `ExecutionToolPolicyFinalizer` in `src/runtime/execution-boundary/execution-tool-policy-finalizer.ts`; Session 66 added `ExecutionRunCompletionFinalizer` in `src/runtime/execution-boundary/execution-run-completion-finalizer.ts`; Session 67 added `ExecutionRunResultNormalizer` in `src/runtime/execution-boundary/execution-run-result-normalizer.ts`; Session 68 closed the execution/runtime cleanup line as stable enough to pause as the primary focus in `docs/refactoring/session68-execution-runtime-cleanup-closure-review.md`: the runtime-facing execution boundary and its cycle/preparation/finalization seams are now explicit and stable, the remaining `ExecutionService` pressure is mostly orchestration ownership rather than one more obvious import-pressure knot, one narrow app-layer result-type reach-through remains deferred, and the next architectural focus should shift away from further `ExecutionService` peeling. |
| RF-034 | Infra-hub reduction | Reduce `src/infra` as super dependency hub | done | low | RF-033 | 77 | Session 69 identified the direct `LLMProviderManager` -> `gatewayEventBus` streaming back-edge as the first concrete RF-034 target; Session 70 completed that narrow dependency-direction repair in `docs/refactoring/session70-llm-stream-event-sink-boundary.md`; Session 71 then selected the next narrow target in `docs/refactoring/session71-rf034-next-target-review.md`; Session 72 completed that first coding step in `docs/refactoring/session72-conversation-bootstrap-boundary-extraction.md` by extracting the default conversation-runtime assembly behind a scheduler-owned bootstrap boundary while preserving gateway behavior, IPC, scheduler-authoritative materialization, and current worker/runtime ownership lines; Session 73 then completed the next discovery/design review in `docs/refactoring/session73-scheduler-composition-root-placement-review.md`, identifying the gateway-named scheduler factory/composition-root placement as the single highest-value remaining RF-034 target; Session 74 then introduced `src/scheduler/composition/default-scheduler.ts` as the scheduler-owned default `SchedulerCore` composition entry point, retargeted daemon and replay CLI callers to it, and reduced `src/gateway/integration/scheduler-factory.ts` to a compatibility shim without changing scheduler behavior, IPC, worker seams, or runtime ownership lines; Session 75 then completed the next narrow scheduler/gateway seam cleanup in `docs/refactoring/session75-scheduler-repository-boundary-cleanup.md` by moving the concrete `SchedulerRepositoryAdapter` implementation under `src/scheduler/composition/`, retargeting scheduler-owned composition to that scheduler-owned boundary, and keeping gateway export/import compatibility shims without changing repository behavior, execution behavior, replay behavior, or runtime ownership lines; Session 76 then completed the next RF-034 consolidation pass in `docs/refactoring/session76-gateway-compatibility-surface-consolidation.md` by moving the concrete legacy `ExecutionEngineAdapter` under `src/scheduler/composition/`, reducing the gateway-side file to a compatibility shim, and retargeting gateway compatibility barrels so scheduler-owned adapter exports resolve directly to scheduler/runtime-owned targets while preserving runtime behavior, daemon behavior, replay behavior, and current ownership lines; Session 77 completed the closure attempt in `docs/refactoring/session77-rf034-closure-attempt.md` by consolidating the remaining gateway scheduler factory/adapter compatibility exports into one intentional `src/gateway/integration/scheduler-compatibility.ts` layer, leaving only thin import-preserving shells and true gateway boundaries, and confirming scheduler/runtime-owned paths no longer depend on gateway compatibility surfaces for scheduler-owned concerns. |
| RF-035 | Gateway/daemon seam cleanup | Further align code boundaries with runtime process boundaries | done | low | RF-033 | 82 | Session 78 started the gateway/daemon transport-boundary block by extracting the daemon-owned `IDaemonEventEmitter` and `DaemonEventEmitterMixin` into `src/autonomy/daemon-event-emitter.ts`; Session 79 then consolidated the `GatewayServer.connectDaemon(...)` area by introducing gateway-owned `GatewayDaemonAttachment` and `daemon-event-forwarding` modules, routing `GatewayServer` through that explicit attachment boundary, and reducing `src/gateway/integration/daemon-bridge.ts` to a thin compatibility shell; Session 80 then tightened the daemon-attachment public surface by adding gateway-owned `GatewayDaemonAttachmentStatus` / `GatewayDaemonAttachmentSurface`, routing gateway/system status reporting through attachment-owned snapshots, and separating intended live attachment exports from compatibility shell exports; Session 81 then prepared the detach-facing structural boundary by extracting gateway-owned daemon-binding lifecycle bookkeeping into `src/gateway/integration/gateway-daemon-lifecycle.ts`; Session 82 completed the structural transport-boundary closure in `docs/refactoring/session82-detach-operation-boundary-closure-attempt.md` by introducing `src/gateway/integration/gateway-daemon-detach-operations.ts`, making detach-facing operation state explicit on the live `GatewayDaemonAttachment` boundary, routing `GatewayServer` internal daemon status reads through attachment-owned operation state, and leaving `DaemonBridge` as compatibility-only without changing daemon attach, detach, or unsubscribe semantics. |
| RF-036 | Event protocol cleanup | Review legacy `task.*` event names and future protocol normalization | deferred | low | RF-020 | 121 | Session 115 started RF-036 in `docs/refactoring/session115-rf036-event-protocol-review.md`; Session 116 then completed the first major coding cluster in `docs/refactoring/session116-rf036-gateway-compatibility-boundary.md` by separating gateway/TUI `task.narration` / `task.result` into an explicit compatibility-only boundary and tightening the live gateway type surface around the authoritative `goal.*` / `workitem.*` / `run.*` / `verification.*` protocol; Session 117 then completed the bounded line review in `docs/refactoring/session117-rf036-line-review.md` and concluded RF-036 should pause because the only substantial remaining candidate is the runtime-internal `task.ready` seam; Session 121 then re-reviewed that seam in `docs/refactoring/session121-rf036-task-ready-review.md` and confirmed RF-036 should remain fully paused because the only safe remaining moves are low-yield authoritative-classification/typing polish, while any meaningful alias/rename would drift into higher-risk runtime protocol, replay, and event-store migration. |
| RF-055 | Runtime tooling context extraction | Introduce an explicit scheduler-owned runtime tooling context and stop using `globalToolProvider` as the runtime-core source of truth | done | medium | RF-032 | 55 | Session 55 added `RuntimeToolingContext` as the first explicit runtime-owned tooling boundary, made it authoritative for `ExecutionService`/`ReActIntegration` and the first planning/conversation consumers, kept legacy prompt/tool singletons as compatibility mirrors only, and preserved gateway, IPC, worker seams, and execution semantics. |
| RF-056 | LLM streaming back-edge cleanup | Remove the direct `LLMProviderManager` dependency on gateway event publication by introducing a narrow injected streaming event sink/adapter boundary | done | medium | RF-034 | 70 | Session 70 introduced `src/infra/llm/provider-manager/stream-event-sink.ts`, rewired `LLMProviderManager` to depend on that injected boundary with a no-op default, moved concrete `gatewayEventBus.emit(...)` mapping into `src/gateway/events/llm-stream-event-sink.ts`, and preserved existing `llm.stream.*` event names, payload semantics, fallback behavior, and streaming callbacks. |
| RF-057 | Session-intake composition boundary | Extract the default conversation-runtime assembly boundary from `SchedulerSessionIntake` so the intake facade no longer constructs the full conversation graph inline | done | medium | RF-056 | 72 | Session 71 identified this as the highest-value next RF-034 step after the provider-manager streaming back-edge removal; Session 72 completed it by introducing `src/scheduler-daemon/conversation-bootstrap/default-conversation-bootstrap.ts`, moving the default persona/repository/memory/response/task-bridge/session-manager/worker wiring out of `SchedulerSessionIntake`, keeping tooling-adjacent response-enforcement selection inside the bootstrap boundary, and preserving gateway behavior, IPC, scheduler-authoritative materialization, and the existing `ConversationWorker`/`ToolWorker` seams. |
| RF-058 | Daemon detach boundary prep | Add a narrow gateway-owned detach-facing structural boundary after the attachment/reporting surface is explicit | done | low | RF-035 | 82 | Session 81 started this line in `docs/refactoring/session81-detach-facing-boundary-preparation.md` by extracting gateway-owned daemon-binding lifecycle bookkeeping into `src/gateway/integration/gateway-daemon-lifecycle.ts`; Session 82 completed it in `docs/refactoring/session82-detach-operation-boundary-closure-attempt.md` by introducing `src/gateway/integration/gateway-daemon-detach-operations.ts`, exposing `getDetachStatus()` / `getOperationState()` from the live `GatewayDaemonAttachment` boundary, and keeping current daemon detach/unsubscribe semantics explicitly represented as unsupported rather than implicit or scattered. |
| RF-059 | Compatibility/public-surface rationalization | Reduce structural confusion across gateway integration/public barrels and historical compatibility shims while preserving import compatibility | done | medium | RF-035 | 85 | Session 83 started this new block in `docs/refactoring/session83-compatibility-public-surface-rationalization.md` by classifying the remaining gateway/integration compatibility surfaces, introducing explicit live vs compatibility barrels in `src/gateway/integration/` and `src/gateway/`, and rerouting the historical mixed barrels through those explicit modules without changing runtime behavior or historical named-export compatibility; Session 84 then continued the same block in `docs/refactoring/session84-historical-shim-thinning-and-root-export-narrowing.md` by extracting `src/gateway/integration/daemon-compatibility.ts` as the explicit daemon-side compatibility layer, reducing `src/gateway/integration/daemon-bridge.ts` to a direct shim, and keeping the live `GatewayDaemonAttachment` boundary/public routing unchanged; Session 85 closed the block in `docs/refactoring/session85-public-root-export-closure-attempt.md` by adding explicit root `src/public.ts` and `src/compatibility.ts` surfaces, reducing `src/index.ts` to a thin historical mixed barrel, and concluding the remaining mixed gateway/root routes are now acceptable final public or compatibility boundaries rather than active structural debt. |
| RF-060 | Startup/bootstrap composition-root rationalization | Clarify ownership between startup wiring, composition helpers, runtime modules, and public entrypoints across daemon/gateway/root startup paths | done | medium | RF-059 | 88 | Session 86 started this new block in `docs/refactoring/session86-startup-bootstrap-composition-root-rationalization.md` by classifying root, gateway, daemon, and CLI startup-adjacent surfaces, then extracting `src/scheduler-daemon/bootstrap/default-daemon-runtime.ts` as the explicit daemon startup/bootstrap assembly module; Session 87 continued the same block in `docs/refactoring/session87-gateway-startup-bootstrap-rationalization.md` by extracting `src/gateway/bootstrap/default-gateway-runtime.ts` as the explicit gateway startup/bootstrap assembly module and rerouting `src/cli/commands/gateway.ts` through it; Session 88 closed the block in `docs/refactoring/session88-rf060-closure-attempt.md` by introducing `src/gateway/bootstrap/gateway-server-runtime-lifecycle.ts`, moving startup-only/shutdown-only runtime sequencing out of `GatewayServer`, making default scheduler-socket bootstrap input explicit on the live gateway bootstrap path, and concluding the remaining `GatewayServer` constructor assembly is true runtime ownership rather than unresolved startup/bootstrap debt. |
| RF-061 | Gateway runtime graph / service-wiring rationalization | Clarify GatewayServer-adjacent steady-state runtime ownership versus internal graph assembly/service wiring without changing runtime semantics | done | medium | RF-060 | 94 | Session 89 started this new block in `docs/refactoring/session89-gateway-runtime-graph-service-wiring-rationalization.md` by extracting the gateway channel runtime family; Session 90 continued it in `docs/refactoring/session90-rollout-telemetry-rollback-wiring-rationalization.md` by introducing `src/gateway/runtime/gateway-runtime-rollout-coordinator.ts`, moving rollout telemetry subscription wiring, threshold evaluation, rollback-to-legacy mutation, scheduler rollout forwarding, and dry-run failure coordination out of `GatewayServer`; Session 91 continued it in `docs/refactoring/session91-scheduler-event-audit-wiring-rationalization.md` by introducing `src/gateway/runtime/gateway-scheduler-event-audit-observer.ts`, moving scheduler-derived event audit subscription wiring, event-set ownership, and teardown bookkeeping out of `GatewayServer` while preserving the existing event bus, audit persistence, startup lifecycle hooks, and runtime behavior; Session 92 continued it in `docs/refactoring/session92-tool-provider-wiring-rationalization.md` by introducing `src/gateway/runtime/gateway-tool-provider-runtime.ts`, moving built-in tool registration, baseline allowlist seeding, gateway-local `ToolEnforcer` / `ToolProvider` assembly, legacy global tool-provider installation, and gateway-owned LLM stream-sink binding out of `GatewayServer` while preserving the existing tool registry surface, runtime behavior, and runtime-tooling-context source-of-truth rules on migrated paths; Session 93 continued it in `docs/refactoring/session93-remaining-mixed-service-wiring-review-and-landing.md` by introducing `src/gateway/runtime/gateway-runtime-rpc-surface.ts`, moving the mixed `system.*` / `internal.runtime.*` dependency assembly, gateway status snapshot wiring, channel admin update bridging, rollout callback exposure, realtime IPC metric exposure, and scheduler model-override bridge/fallback wiring out of `GatewayServer` while preserving RPC behavior and steady-state runtime semantics; Session 94 then completed the closure review in `docs/refactoring/session94-rf061-closure-review.md`, concluding the remaining inline `GatewayServer` surfaces are acceptable steady-state runtime ownership or thin delegation into already-extracted helpers, so no justified final extraction remained and `RF-061` could be closed without further code churn. |
| RF-062 | Gateway runtime observation/wiring follow-up | Reduce the next mixed GatewayServer runtime-observation/service-wiring cluster after the channel runtime extraction | done | low | RF-061 | 111 | Session 110 completed the line's one clearly high-value coding cluster in `docs/refactoring/session110-rf062-tool-provider-runtime-wiring.md` by extracting the `GatewayToolProviderRuntime` assembly/publication seam into `gateway-tool-provider-runtime-cluster`; Session 111 then completed the bounded line review in `docs/refactoring/session111-rf062-line-review.md` and concluded RF-062 should pause here because no further clearly high-value, tightly bounded, semantics-preserving GatewayServer-adjacent cluster remains, with Session 112 better spent starting bounded RF-030 materialization review/design work. |
| RF-063 | Cross-runtime source-of-truth review | Perform the first bounded discovery/review for cross-runtime source-of-truth rationalization and identify the single highest-value first target | done | medium | RF-061 | 95 | Session 95 completed the first bounded review in `docs/refactoring/session95-cross-runtime-source-of-truth-review.md`, explicitly stopped short of reopening `RF-034` / `RF-059` / `RF-060` / `RF-061`, and identified effective model-selection authority as the highest-value current source-of-truth target: runtime overrides + agent model hints + `llm-config` workload/tier resolution should be the authority, while TUI state, persisted `selected_model` / `model` metadata, run-event payloads, and transport-facing reads should become mirror/projection consumers only. |
| RF-064 | Effective model-selection authority rationalization | Introduce one explicit effective-model-resolution authority boundary across runtime config, agent config, scheduler selection, and persisted mirrors without changing runtime semantics | done | medium | RF-063 | 100 | Session 96 began the first bounded implementation in `docs/refactoring/session96-effective-model-resolution-boundary.md` by adding `src/infra/llm/provider-manager/effective-model-resolution.ts` as the explicit authority read path for runtime override -> agent runner hint -> `llm-config` tier precedence, rewiring provider-manager workload resolution plus conversation bootstrap to use it, and centralizing the scheduler pre-execution default leg through the same boundary while preserving existing persisted `selected_model` / `model` mirrors, `model_source` values, RPC/event payload shapes, startup semantics, scheduler semantics, and provider execution behavior; Session 97 then continued that line in `docs/refactoring/session97-session-preferred-model-alignment.md` by routing `SessionManager` preferred-model reads for analysis, response generation, progress narration, and memory indexing through the same authority helper while intentionally preserving the existing `string | undefined` session-facing shape, leaving TUI mirror cleanup and persisted compatibility/projection normalization out of scope; Session 98 then added the first bounded compatibility projection/materialization boundary in `docs/refactoring/session98-model-metadata-projection-hygiene.md` via `src/infra/llm/provider-manager/model-selection-compatibility.ts`, rewiring the run-facing scheduler/direct-execution materializers plus the nearby conversation/RPC selected-model mirrors while preserving field names, runtime semantics, and current `model_source` compatibility behavior; Session 99 then continued the same line in `docs/refactoring/session99-replay-direct-execution-projection-alignment.md` by aligning the remaining replay-path `run_started` and direct-execution run-creation model metadata projections to the shared compatibility boundary while preserving replay payload shapes, direct execution run-context shapes, and existing runtime semantics; Session 100 completed the bounded review in `docs/refactoring/session100-source-of-truth-line-review.md` and concluded the line should pause here as structurally valuable but now at diminishing returns, because the remaining plausible targets are mostly TUI/transport mirrors or legacy compatibility residue rather than another clearly semantics-preserving first-priority cleanup. |
| RF-065 | Runtime-core singleton / service-locator review | Perform the first bounded discovery/review for broader runtime-core singleton / service-locator cleanup and identify the single highest-value first target | done | medium | RF-064 | 103 | Session 101 completed the initial bounded review in `docs/refactoring/session101-runtime-core-singleton-review.md` and selected the `LocalExecutionAdapter` agent-tick path as the highest-value first target; Session 103 then completed the follow-up review in `docs/refactoring/session103-runtime-core-singleton-line-review.md` and concluded the line should pause after Session 102 because the remaining live singleton targets are either lower-yield or too entangled with paused source-of-truth work, closed gateway/runtime-wiring work, startup semantics, or compatibility churn to justify another bounded coding session here. |
| RF-066 | Agent-tick execution registry boundary | Introduce a narrow injected agent-tick registry/runner dependency boundary for `LocalExecutionAdapter` so the live execution boundary no longer reads global agent/runner registries directly | done | medium | RF-065 | 103 | Session 102 completed the first coding phase for this target in `docs/refactoring/session102-local-execution-adapter-boundary-completion.md` by adding an explicit `LocalExecutionAgentTickResolver`, removing the direct `getGlobalAgentRegistry()` / `getGlobalRunnerRegistry()` reads from `LocalExecutionAdapter`, and rewiring both `src/scheduler/composition/default-scheduler.ts` and `src/scheduler-daemon/bootstrap/default-daemon-runtime.ts` to inject the resolver while preserving runner selection, definition-hash comparison, and fallback-to-`ExecutionService` behavior; Session 103 then confirmed this was the line's last clearly high-value bounded step and recommended pausing broader singleton/service-locator cleanup here rather than forcing a weaker follow-up target. |
| RF-067 | Daemon detach/unsubscribe capability design | Perform the first bounded review/design session for daemon detach/unsubscribe capability using the explicit gateway detach-facing boundary as the starting point | done | medium | RF-058 | 104 | Session 104 completed the bounded review in `docs/refactoring/session104-daemon-detach-capability-design.md`, defined the live baseline around `GatewayDaemonAttachment` / `gateway-daemon-detach-operations`, separated detach from daemon unsubscribe and existing session-level unsubscribe APIs, and selected one safest first future slice: add releasable daemon event-subscription bindings behind the existing gateway attachment boundary without changing current RPC, IPC, startup, scheduler, or runtime semantics. |
| RF-068 | Releasable daemon event-subscription binding | Introduce releasable daemon callback bindings and grouped gateway-owned forwarding cleanup as the first structural precondition for future detach capability | done | medium | RF-067 | 106 | Session 105 completed Phase 1 in `docs/refactoring/session105-releasable-daemon-event-bindings.md` by changing `IDaemonEventEmitter` registrations to return releasable handles, making `registerDaemonEventForwarders(...)` return one grouped binding cleanup, and having `GatewayDaemonAttachment` retain that binding internally without adding public detach commands, changing detach/status payload shapes, or altering runtime/IPC behavior; Session 106 then confirmed in `docs/refactoring/session106-daemon-detach-phase2-review.md` that Phase 1 is complete and should be followed by one internal-only detach slice that releases the grouped binding and resets gateway attachment state without exposing new public control-plane semantics. |
| RF-069 | Internal gateway-owned daemon detach | Add one internal-only detach operation that releases grouped daemon-event forwarders and resets gateway attachment lifecycle state without exposing a public detach command | done | medium | RF-068 | 107 | Session 107 completed Phase 2 in `docs/refactoring/session107-internal-detach-with-lifecycle-reset.md` by adding one attachment-owned internal `detach()` seam, invoking grouped forwarding release through the Phase 1 binding boundary, resetting `GatewayDaemonLifecycle` back to the existing detached snapshot, preserving current public detach/status shapes and unsupported support flags, and keeping public/control-plane detach work for Phase 3. |
| RF-070 | Daemon detach control-plane entrypoint | Add one narrow internal/admin/runtime detach request path that invokes the existing gateway-owned detach seam without broadening daemon lifecycle semantics | done | medium | RF-069 | 109 | Session 108 completed Phase 3 in `docs/refactoring/session108-detach-control-plane-entrypoint.md` by registering admin-only `internal.runtime.daemon.detach` through `GatewayRuntimeRpcSurface` and `registerInternalRuntimeHandlers(...)`, routing it to the existing `GatewayDaemonAttachment.detach()` seam, preserving attach/event/status shapes, keeping daemon unsubscribe unsupported, and updating `detachSupported` to `true` because detach is now reachable through an existing runtime control-plane surface; Session 109 then completed the bounded line review in `docs/refactoring/session109-detach-capability-line-review.md` and concluded the detach capability block should pause here because the remaining plausible follow-ups are mostly public rollout, unsubscribe/protocol work, or UI/reporting polish rather than another clearly high-value bounded structural slice, with RF-062 now the better Session 110 target. |
| RF-071 | Runtime tooling compatibility boundary | Rationalize legacy prompt/tool fallback ownership around `RuntimeToolingContext` without changing runtime semantics | deferred | medium | RF-055 | 120 | Session 118 selected this as the next active block in `docs/refactoring/session118-major-block-reranking-review.md`; Session 119 then completed the line's one clearly high-value coding cluster in `docs/refactoring/session119-runtime-tooling-compatibility-boundary.md` by introducing `src/infra/prompts/legacy-prompt-tooling-compatibility.ts`, moving `PromptProvider` default/global fallback construction behind that seam, and routing both `RuntimeToolingContext.syncLegacyGlobals()` and `GatewayToolProviderRuntime` legacy-global installation through the same compatibility owner; Session 120 then completed the bounded line review in `docs/refactoring/session120-rf071-line-review.md` and concluded the remaining candidates are compatibility residue or singleton-adjacent drift rather than another clearly high-value bounded slice, so RF-071 should pause and be re-ranked. |
| RF-072 | Agent registry boundary | Introduce an explicit agent-definition access boundary for live read/materialization consumers without changing startup, transport, or runtime semantics | in_progress | medium | RF-066 | 124 | Session 122 selected this as the best next major block in `docs/refactoring/session122-major-block-reranking-review.md`; Session 123 then completed the bounded seam review in `docs/refactoring/session123-agent-registry-boundary-review.md`; Session 124 completed the first major coding cluster in `docs/refactoring/session124-agent-registry-readonly-boundary.md` by adding `src/infra/agents/agent-definition-read-access.ts`, rewiring `SessionManager` and `WorkloadModelResolver` to the new read-only boundary, and explicitly leaving `agent.command.submit`, daemon startup loading/cron reconciliation, scheduler composition fallback, and runner registration on their existing ownership paths pending the next review. |
| RF-037 | Manual replay design | Produce safe manual replay design doc for evented execution runs | done | high | RF-019 | 24 | Session 24 defined manual replay as creating one replacement run for the same work item, with original-run continuation suppression and narrow replay lineage in `evented_dispatch`. |
| RF-038 | Manual replay action 1 | Implement the safest first manual replay action chosen by RF-037 | done | high | RF-037 | 25 | Session 25 added `pb scheduler replay-run <runId>` for the first safe evented-only manual replay path: original-run continuation is durably suppressed in `evented_dispatch.manual_replay`, one replacement run is created on the same work item with replay lineage, late original results are suppressed at continuation-claim time, and direct mode behavior remains unchanged. |
| RF-039 | Replay inspection | Add narrow read-only replay lineage/state inspection for operators | done | medium | RF-038 | 26 | Session 26 extended `pb scheduler inspect-run <runId>` so operators can inspect replay lineage from either side via explicit replay fields and derived lineage role/peer output, without changing replay or recovery semantics. |
| RF-040 | Manual replay hardening | Harden replay eligibility and duplicate replay suppression for the existing manual replay flow | done | high | RF-038 | 27 | Session 27 tightened durable replay gating, rejects replay attempts and missing/ineligible targets explicitly, blocks duplicate replacement creation using both original-side and reverse-lineage checks, and improves CLI replay rejection reporting without changing direct mode, gateway, IPC, or broader recovery scope. |
| RF-041 | Replay precheck surface | Add a narrow read-only replay precheck / confirmation CLI surface before manual replay execution | done | medium | RF-040 | 28 | Session 28 added `pb scheduler replay-precheck <runId>` as a read-only operator surface that reuses the same durable replay eligibility classifier as `replay-run`, reports stable rejection codes or expected replay consequences, and preserves direct mode plus existing replay execution semantics. |
| RF-042 | Replay post-run diagnostics | Add a narrow read-only replay outcome / replay-chain inspection surface after manual replay execution | done | medium | RF-041 | 29 | Session 29 extended `pb scheduler inspect-run <runId>` with a read-only replay outcome block derived from durable lineage plus linked peer inspection, so operators can determine replay initiation, replacement-run status, continuation suppression, replacement continuation application, and whether the chain currently appears active, completed, failed, or unresolved without changing replay behavior. |

## 3. Suggested Near-Term Sequence

| Order | Task ID | Reason |
|---|---|---|
| 1 | RF-017 | Define the safe manual recovery action model before implementing any actual recovery action. |
| 2 | RF-018 | Implement the single safest manual recovery action chosen by RF-017. |
| 3 | RF-019 | Add the second conservative operator action only if still justified. |
| 4 | RF-020 | Reassess whether evented mode is approaching default-ready. |
| 5 | RF-021 | Start the next large architecture front: ToolWorker design. |

## 4. Session Update Instructions for Codex (Use from Session 22 onward)

Include the following in future implementation prompts:

```text
Also update:
- docs/refactoring/ponybunny_refactor_master_task_list.md

Update rules:
- Update only the task row(s) relevant to this session.
- Set or adjust: status, last_session, notes.
- If this session completes a planned task, mark it as `done`.
- If this session starts but does not finish a task, mark it as `in_progress`.
- If this session discovers a necessary follow-up task not yet listed, append one new row with the next available Task ID.
- Do not rewrite unrelated rows.
- Keep task IDs stable.
```

## 5. Status Legend Guidance

| Status | Meaning |
|---|---|
| `done` | Implemented and validated for the intended scope. |
| `in_progress` | Active task with partial completion. |
| `planned` | Known future task, not yet started. |
| `blocked` | Cannot proceed until dependency or design issue is resolved. |
| `deferred` | Intentionally postponed. |
