# PonyBunny Web UI - 快速启动指南

## 🚀 启动步骤

### 1. 确保后端服务运行

```bash
# 启动Gateway（必需）
pb gateway start

# 或启动所有服务
pb service start all

# 检查状态
pb service status
```

### 2. 启动Web开发服务器

```bash
cd web
npm install   # 首次运行
npm run dev
```

打开浏览器访问: **http://localhost:3000**

## 📱 界面功能

### 1. 对话页面 (/)
- 默认首页，简洁的聊天界面
- **实时流式响应** - LLM回复逐字显示
- 完整的Markdown支持
- 自动连接Gateway后端

### 2. 系统状态 (/status)
- 实时显示Gateway和Scheduler状态
- 一键启动/停止/重启服务
- 显示服务PID和运行信息
- 每5秒自动刷新

### 3. 配置管理 (/config)
- 查看和编辑三个配置文件：
  - **credentials.json** - API密钥
  - **llm-config.json** - LLM配置
  - **mcp-config.json** - MCP服务器配置
- JSON格式验证
- 实时显示修改状态

## ⚙️ 前置条件

### 必需:
✅ Gateway服务运行 (`pb gateway start`)
✅ Node.js 20+ 
✅ npm 或 yarn

### 可选（用于系统状态页面）:
✅ `pb` CLI在系统PATH中
✅ 配置文件已初始化 (`pb init`)

## 🔧 故障排查

### 问题1: "Failed to connect to Gateway"
**解决**:
```bash
# 检查Gateway是否运行
pb gateway status

# 如果未运行，启动它
pb gateway start
```

### 问题2: 系统状态页面显示"No services detected"
**原因**: `pb`命令不在PATH中

**解决**:
```bash
# 方法1: 构建CLI并链接
cd ..
npm run build:cli
npm link

# 方法2: 使用绝对路径
which pb
```

### 问题3: 配置页面无法加载文件
**解决**:
```bash
# 初始化配置文件
pb init

# 检查配置目录
ls -la ~/.ponybunny/
```

### 问题4: 流式响应不工作
**检查**:
1. Gateway正在运行
2. 浏览器控制台没有SSE错误
3. 网络标签页显示`/api/events`连接

## 🧪 测试清单

- [ ] 访问 http://localhost:3000 自动重定向到 /chat
- [ ] 在对话页面发送消息，看到流式响应
- [ ] 切换到系统状态页面，看到服务状态
- [ ] 点击启动/停止按钮，服务状态更新
- [ ] 切换到配置页面，查看配置文件
- [ ] 编辑配置并保存，看到成功提示

## 📝 开发说明

### 文件结构
```
web/src/
├── app/
│   ├── layout.tsx           # 主布局 + Sidebar
│   ├── page.tsx             # 重定向到 /chat
│   ├── chat/page.tsx        # 对话页面
│   ├── status/page.tsx      # 系统状态
│   └── config/page.tsx      # 配置管理
├── components/
│   └── layout/sidebar.tsx   # 侧边栏导航
└── lib/
    └── api-client.ts        # API + SSE客户端
```

### 技术栈
- Next.js 16 (App Router)
- React 19
- TypeScript (严格模式)
- Tailwind CSS 4
- shadcn/ui组件

### 构建生产版本
```bash
npm run build
npm start
```

## 🎯 核心特性

✅ **实时流式响应** - Server-Sent Events (SSE)
✅ **服务管理** - 通过pb CLI控制
✅ **配置编辑** - 带验证的JSON编辑器
✅ **类型安全** - 完整TypeScript支持
✅ **响应式设计** - 桌面优先布局

## 📚 相关文档

- [新架构详细说明](./NEW_UI_ARCHITECTURE.md)
- [PonyBunny README](../README.md)
- [Gateway设计文档](../docs/techspec/gateway-design.md)

---

**Last Updated**: 2026-02-10
